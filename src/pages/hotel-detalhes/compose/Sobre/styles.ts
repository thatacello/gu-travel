import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  height: unset;
  margin-bottom: 7rem;

  .title {
    font-size: 1.6rem;
    margin: 0.5rem 0 2rem 0;
  }
  .subtitle {
    font-size: 1.25rem;
    margin: 0.5rem 0 2rem 0;
  }
`;